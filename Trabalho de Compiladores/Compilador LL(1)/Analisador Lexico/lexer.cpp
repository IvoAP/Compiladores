#include "tokens.cpp"
#include<vector>
#include<utility>

class lexer {
    private:
        std::vector<std::pair<std::string, std::string>> tokens;
    public:
        bool lex(std::string) {
            std::string temp;
        }
};